# Git bash setting on windows10
export LANG=ja_JP.UTF-8
alias percol='fzf'
alias ls='ls --color=auto'
alias Open='explorer.exe'
alias vim='/C/Windows/vim.bat' # using choco vim instead of git bash vim

# win sudo setting
[ -f "${HOME}/bin/win-sudo/s/path.sh" ] && source "${HOME}/bin/win-sudo/s/path.sh"

